/**
 * DECRIPTION
 *
 * @prop {object}  - PROP_DESCRIPTION
 */

class SideNav extends React.Component {
  constructor(props) {
    super(props);
    // this.state = {  };
  }

  render() {
    // console.log('[sidenav.js] props: ', this.props);
    // console.log('[sidenav.js] state: ', this.state);

    // let {  } = this.props;
    // let {  } = this.state;

    return (
      <div>
      </div>
    );
  }
}

SideNav.displayName = 'SideNav';

// SideNav.propTypes = {
//   : React.PropTypes.object
// };

module.exports = SideNav;
